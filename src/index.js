// External dependencies
import React from 'react';
import ReactDOM from 'react-dom';

// Internal dependencies
import App from 'App';

// Attach App component to react render tree
ReactDOM.render(
	<React.StrictMode>
		<App />
	</React.StrictMode>,
	document.getElementById('root')
);
